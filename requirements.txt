# Backend Dependencies
# Installation with uv: uv sync
# Installation with pip: pip install -r requirements.txt

fastapi>=0.124.0
uvicorn>=0.38.0
supabase>=2.0.0
python-dotenv>=1.0.0

# Topic Modeling with Gensim
gensim>=4.3.0

# Excel Processing
pandas>=2.0.0
openpyxl>=3.1.0

# File Upload Support
python-multipart>=0.0.6

# Sentiment Analysis
# Lexicon-based: No dependencies (default)
# Transformer-based: Requires transformers and torch (optional)
transformers>=4.30.0  # For ML-based sentiment analysis
torch>=2.0.0  # PyTorch backend for transformers

# Note: uv itself is installed separately as a system-level tool
# Installation: https://docs.astral.sh/uv/getting-started/installation/
# macOS/Linux: curl -LsSf https://astral.sh/uv/install.sh | sh
# Windows: powershell -c "irm https://astral.sh/uv/install.ps1 | iex"
